.DS_Store

# fedit/ is a standalone repo (its own .git) — managed separately.
fedit/

# Editor / tool config — local only, never publish.
.claude/
.vscode/
.idea/

# Internal working notes — never publish.
logbook.md
todo.md
